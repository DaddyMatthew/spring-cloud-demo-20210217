<?xml version="1.0" encoding="ISO-8859-1"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserMapper">

    <insert id="addUser"
            parameterType="com.example.dto.UserAddDto"
            useGeneratedKeys="true"
            keyProperty="user.id">
        insert into demo_user (name, password, gender, created_date)
        values (#{user.name}, #{user.password}, #{user.gender}, current_timestamp)
    </insert>


</mapper>
